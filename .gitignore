# Sample / fixture data (HAR, downloaded markdown, etc.)
**/*.har

# macOS
.DS_Store
**/.DS_Store

# Dependencies (if added later)
node_modules/

# Build / IDE
*.log
.env
.env.local

# Local PDF extractor test data
flow/src/flow/test_data/*
!flow/src/flow/test_data/.gitkeep
